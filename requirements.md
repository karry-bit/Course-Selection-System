# 大学生选课系统 — 需求文档

## 一、项目概述
本项目为基于 Spring Boot（后端）与 Vue（前端）的大学生选课系统。目标为替代人工/低效流程，提供学生选课、退课、成绩查询与课程表管理功能，并为管理员/教师提供课程、学生、教师管理与统计分析功能。

## 二、技术栈
- 前端：Vue 3、Vite、Tailwind CSS、shadcn/ui 风格组件、ECharts（图表）
- 后端：Spring Boot、Spring Data JPA（或 MyBatis）、RESTful API
- 数据库：MySQL（或 PostgreSQL）
- 构建/部署：Maven/Gradle、Docker（可选）

## 三、主要用户与角色
- 管理员（Admin）
- 教师（Teacher）
- 学生（Student）

## 四、功能需求
### 管理员功能（必需）
- 学生管理：增、删、改、查学生信息、批量导入（CSV/Excel）
- 课程管理：增、删、改、查课程信息（课程名称、学分、上课时间、容量、教师）
- 教师管理：增、删、改、查教师信息、分配授课
- 选课管理：开放/关闭选课时段、修改选课时间、设置容量与限制、手动调整选课记录
- 成绩管理：录入、修改、批量导入成绩，支持成绩导出
- 班级管理：增删改查班级信息及学生归属
- 课程表管理：为学生/班级生成课程表并支持调整
- 统计分析：选课热度、学生选课分布、成绩分布等图表（ECharts）

### 学生功能（必需）
- 个人信息管理：查看/编辑个人信息（受限）
- 选课：浏览课程、搜索与筛选、选择与退选课程
- 实时冲突检测：选课时检查与已选课程时间冲突并提示替代方案
- 成绩查询：查看个人成绩历史
- 课程表查看：查看按周/按天的课程表

### 教师功能（必需）
- 个人信息管理：查看/编辑个人信息
- 教授课程管理：管理自己教授的课程信息（说明/资料/评分规则）
- 成绩管理：录入与修改所教课程学生成绩

## 五、非功能需求
- 响应式：支持桌面/平板/手机（布局、侧轨折叠/展开）
- 可用性：界面直观、操作反馈明确（Toast/Modal）
- 性能：列表分页或虚拟滚动，图表按需加载，后端接口支持分页与筛选
- 安全：基于角色的权限控制（RBAC），接口鉴权（JWT 或 Session），输入校验与防注入
- 可维护性：模块化前端组件（遵循 `spec.md`），清晰后端分层（Controller/Service/Repository）
- 可测性：单元测试、集成测试与 E2E 测试覆盖关键路径（选课流程、成绩录入）

## 六、UI/UX 与实现约束（前端规范要点）
- 组件语言：统一使用 `Card` / `Badge` / `Button` / `Dialog` / `Toast` / `ChartCard` / `SideRail`（参照 `spec.md`）
- 风格框架：使用 `shadcn/ui` 风格 + Tailwind（不要使用 MUI 做弹层/卡片/布局）
- 图表：使用 ECharts；侧轨为小尺寸概览图（sparkline/mini bar），详情页为完整图
- 侧轨风格：统计区与趋势图采用轻量的 dashboard side-rail 风格
- 响应式：侧轨在移动端折叠为底部或抽屉触发；卡片在小屏单列展示
- 可复用性：组件需支持 props/config 复用，组件目录化管理
- 无障碍：可键盘导航、语义标签、颜色对比符合 WCAG 一般要求（≥4.5:1）

## 七、数据模型（概要）
- Student: id, 学号(studentNo), 姓名, 性别, 出生日期, 班级, 联系方式
- Teacher: id, 工号(teacherNo), 姓名, 职称, 联系方式
- Course: id, 课程号, 名称, 学分, 上课时间(时间段/周次/节次), 地点, 教师Id, 容量, 描述
- Class (班级): id, 名称, 年级, 专业
- Enrollment (选课记录): id, studentId, courseId, status(已选/退选/待审核), 成绩 (nullable), 选课时间
- Term/Session: 选课周期信息（开始/结束/状态）

说明：上课时间应有标准化表示以方便冲突检测（例如：周几、开始节、结束节或时间段）

## 八、API 设计要点（概要）
- RESTful 风格，统一前缀：`/api/v1/...`
- 常见接口：
  - `POST /api/v1/auth/login`（鉴权）
  - `GET /api/v1/courses`（分页/筛选/模糊搜索）
  - `POST /api/v1/enrollments`（选课，后端再次校验冲突/容量）
  - `DELETE /api/v1/enrollments/{id}`（退课）
  - `GET /api/v1/students/{id}/timetable`（课程表）
  - `POST /api/v1/admin/courses`（管理员创建课程）
- 冲突检测：后端在 `POST /enrollments` 中作为最终校验点返回冲突详细信息，前端在发起请求前可进行初步检查以提高体验
- 错误与状态码：使用标准 HTTP 状态码并在响应体中返回统一错误结构 { code, message, details }

## 九、测试与数据初始化
- 提供测试数据脚本（SQL 或 Spring Boot 的 CommandLineRunner）用于演示（教师、学生、课程、选课记录）
- 单元测试：后端 Service/Repository，前端组件关键逻辑
- E2E 测试：使用 Cypress 或 Playwright 覆盖关键用户流程（登录→选课→退课→成绩查询）

## 十、部署与运维要点
- 可使用 Docker 容器化后端与前端；数据库单独容器并备份初始化脚本
- 配置项：数据库连接、JWT 密钥、选课周期（可外部配置）
- 日志与监控：记录关键操作（选课/退课/成绩变更），并收集接口错误率与图表渲染性能指标

## 十一、交付清单与验收标准
- 前端：可运行的 Vue 项目（组件化、符合 `spec.md` 的风格）、ECharts 统计面板、选课/退课 UI 实现
- 后端：Spring Boot 项目（提供上述 REST API、权限控制、冲突校验）
- 数据：演示用测试数据脚本或 SQL 文件
- 文档：`spec.md`（UI 规范）、`requirements.md`（本文件）、部署说明（README）

验收标准示例：
- 管理员能增删改课程与教师、学生；学生能成功选课并在课程表中看到已选课程；教师能录入成绩
- 选课冲突在后端被拦截并返回可读详情；前端给予明显提示
- 仪表盘能展示至少三种图表（选课趋势、课程热度、成绩分布）并在不同屏幕尺寸下保持可用

---

如果你确认此需求文档，我会把它保存为 `requirements.md`（已保存），接下来可以：
- 基于此文档继续实现后端 API 与选课冲突逻辑，或
- 继续完善前端组件并实现 API 调用与前端冲突检测逻辑

请选择下一步或指出需要修改的要点。